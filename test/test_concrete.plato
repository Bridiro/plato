let a: int = 3;
let b: bool = true;

fn mul(x: int, y: int) -> int {
    let r = x * y;
    r
}

fn add(x: int, y: int) -> int {
    x + y
}

fn main() -> int {
    let result = mul(a, 2);
    if true {
        return add(result, 1);
    } else {
        result
    }
}
