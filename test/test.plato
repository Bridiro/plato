let a: i32 = 3;
let b: bool = true;

enum Color {
    Red,
    Green,
    Blue
}

struct Point {
    x: i32,
    y: i32,
}

impl Point {
    fn new(x: i32, y: i32) -> Point {
        Point { x, y }
    }
}

fn mul(x: i32, y: i32) -> i32 {
    x * y
}

fn main() -> i32 {
    let color = Color::Red;
    if b {
        return mul(a, 2);
    }
    a
}
